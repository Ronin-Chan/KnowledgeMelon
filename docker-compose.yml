services:
  backend:
    build:
      context: ./backend
    container_name: knowledge-melon-backend
    env_file:
      - ./backend/.env
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/knowledge_melon
    restart: unless-stopped
